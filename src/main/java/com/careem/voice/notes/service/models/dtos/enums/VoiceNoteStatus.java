package com.careem.voice.notes.service.models.dtos.enums;

/*Enum to limit the status that can be updated by the custoemr app to these statuses only*/
public enum VoiceNoteStatus {
    RECEIVED,
    LISTENED
}
